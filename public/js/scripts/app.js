requirejs(["mongodb"], function (mongodb) {
  // const headerEl = document.getElementById("header");
  // headerEl.textContent = mongodb.upperCase("hello world");

  const { MongoClient, ObjectId } = require('mongodb');
});